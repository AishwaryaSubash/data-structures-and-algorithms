#include <bits/stdc++.h>
using namespace std;

// Check if Strings can be equal
int main()
{
    string s1 = "abcd", s2 = "cdab";
    for (int i = 0; i < s1.length(); i += 2)
    {
        if (s1.at(i)==s2.at(i)) {
            
        }
    }
}