#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    vector<vector<int>> v;
    bool down = true;
    for (int i = 0; i < (n * 2) - 1; i++)
    {
        vector<int> w;
        for (int j = 0; j < (n * 2) - 1; j++)
        {

            if (i < n) {}
            else {}
        }
    }
}